using UnityEngine;
using UnityEngine.EventSystems;

public class UnitDropSlot : MonoBehaviour
{
//    public Vector2Int gridPosition;
//
//    public void OnDrop(PointerEventData eventData)
//    {
//        UnitCardUI card = eventData.pointerDrag?.GetComponent<UnitCardUI>();
//        if (card != null && GameManager.Instance.Player.Money >= card.unitData.cost)
//        {
//            GameManager.Instance.Player.UseMoney(card.unitData.cost);
//            UnitSpawner.Instance.SpawnUnit(card.unitData, gridPosition);
//        }
//    }
}
